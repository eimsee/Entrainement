<div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://img.freepik.com/psd-premium/rendu-3d-pop-corn-temps-du-cinema_252008-391.jpg?w=2000)">
  APERÇU LISTE
</div>
<div class="d-flex flex-row-reverse">
  <%= link_to "Ajouter un film", edit_movie_path, class: "btn btn-flat mt-4 mx-4" %>
  <%= link_to "Supprimer cette liste", list_path(@list), class: "btn btn-flat-bis mt-4",
  data: {turbo_method: :delete, turbo_confirm: "Etes-vous sûr?"}
%>
</div>

<div class="container">
  <h1 class="text-center mb-4"><%= @list.name %></h1>
  <h2 class="card-title text-center"><%= @movie.title %></h2>
  <div class="row mt-5">
    <div class="col-12 col-md-4">
        <img src="<%= @movie.poster_url%>" style='width: 100%; max-height: 60vh; display: block; object-fit: contain;'>
    </div>
    <div class="col-12 col-md-4">
      <ul class="list-group list-group-flush">
        <li class="list-group-item">NOTE: <%= @movie.rating %></li>
        <li class="list-group-item">APERCU: <%= @movie.overview %></li>
      </ul>
    </div>
  </div>

  <hr>
   <div class="d-flex flex-justify-content-center text-center">
  </div>
    <div class="d-flex flex-columnjustify-content-center">
    </div>
  <div class="container mx-autotaine">
  <div class="row">
    <div class="col-12 col-md-6 mt-4">
      <div class="text-center mt-4 mb-4">
        <h1>Ajoutez un nouveau commentaire</h1>
      </div>
        <div class="row justify-content-around">
          <div class="card-center" style="width: 18rem;">
            <ul class="list-group list-group-flush">
              <%= simple_form_for(@list) do |f| %>
              <%= f.input :name, label: "Commentaire" %>
              <%= f.input :name, label: "note" %>
              <%= f.submit "Créer un commentaire" %>
            </ul>
          </div>
            </div>
              <% end %>
    </div>
  </div>
  <div class="row">
    <div class="col-12 col-md-6 mt-4">
      <div class="text-center mt-4 mb-4">
        <h2>COMMENTAIRE</h2>
      </div>
      <hr>
      <div class="text-center mt-4 mb-4">
        <h2>NOTE</h2>
        <div class="container">
        <span id="rateMe4"  class="feedback"></span>
</div>

<!-- rating.js file -->
<script src="js/addons/rating.js"></script>
      </div>
    </div>
  </div>
</div>
